// ports_ps2_pair.v — PORTS fit experiment: PS/2 keyboard + PS/2 mouse only.
// Validates the per-channel cost of the ported GLUE frame engine.

`define PORTS_KEYBOARD
`define PORTS_MOUSE

`include "ports.v"
